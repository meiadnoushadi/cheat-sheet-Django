1-start project
    run : py manage.py makemigrate
    run : py manage.py migrate
2-create superuser
    run : py manage.py createsuperuser
            add name
            add pass(8 ch)

file admin.py application:
1-Create  class inhert admin.ModelAdmin
    list_display : show item record is TUPLE
    list_filter : filter by item record is TUPLE
    search_fields : search in item record is TUPLE
    ordering : ordering record is LIST
    prepopulated_fields : create format item1-item2 for item3 record is DIC={item3:(item1,item2)}
2-register models
    * @admin.register(Auther) used decorator
    * admin.site.register(Auther,AutherAdmin) after class
